// lower_bound(s,e,key,comp) also accepts comparator
// it should be true if first value should come before second value
// by default it is < 
// so if u want to include = numbers before the number and then find the number position 
// write a comparator function returning a<=b
